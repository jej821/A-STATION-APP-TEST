import 'package:flutter/material.dart';

class ClientCenter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("고객센터 :)"),
    );
  }
}
