import 'package:flutter/material.dart';

class ManagementPayment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("결제관리 :)"),
    );
  }
}

